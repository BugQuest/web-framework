<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>404 - {{ __('Donnée Introuvable', 'error', 'fr') }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{ bq_assets_header('global') }}
    {{ bq_assets_header('admin-light') }}

    <style>
        ._tilt {
            display: none;
        }

        ._tilt.active {
            display: inline-block;
        }
    </style>
</head>
<body class="container-flex-center fullvh">
<div class="wrapper">
    <div class="glitch" data-text="⚠️ 404 – {{ __('Donnée Introuvable', 'error', 'fr') }}">
        ⚠️ 404 – {{ __('Donnée Introuvable', 'error', 'fr') }}
    </div>
    <p class="message">
        {{ __("Le fichier recherché semble s’être égaré dans l'internet.", 'error', 'fr') }}
    </p>

    <div class="terminal">
        <div><span class="prompt">> system.scan()</span></div>
        <div>⏳ {{ __('Analyse des dimensions...', 'error', 'fr') }}</div>
        <div>🧩 {{ __("Résultat : Aucun paquet de données trouvé.", 'error', 'fr') }}</div>
        <div><span class="prompt">> status</span></div>
        <div>💥 {{ __("Statut : rupture dans la matrice d’URL.", 'error', 'fr') }}</div>
        <div><span class="prompt">> <span class="_tilt">|</span></span></div>
    </div>
    <script>
        const tilt = document.querySelector('._tilt');
        setInterval(() => {
            tilt.classList.toggle('active');
        }, 500);

    </script>

    <div class="flex-around">
        <a class="button info glow" href="/">
            🔙 {{ __("Revenir à l’accueil", 'error', 'fr') }}
        </a>
        {% include '@framework/partials/language-switcher.twig' %}
    </div>
    {{ bq_assets_footer('global') }}
</div>
</body>
</html>
